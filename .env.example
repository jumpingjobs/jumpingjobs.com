# Cloudflare Web Analytics. The beacon token is public by design and is committed in
# lib/analytics.ts, so nothing is required here for the production build.
#
# Override for a fork or staging site, or set it empty to disable analytics entirely.
# NEXT_PUBLIC_CF_BEACON_TOKEN=
