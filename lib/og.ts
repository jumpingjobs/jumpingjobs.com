/** Open Graph card geometry and alt text, shared by the route that renders it and the
 *  metadata in app/layout.tsx that points at it. */
export const OG_SIZE = { width: 1200, height: 630 } as const;

export const OG_ALT =
  "Jumping Jobs — your job search, run from your agent’s chat. Install with npx jumpingjobs install.";

export const OG_PATH = "/og.png";
